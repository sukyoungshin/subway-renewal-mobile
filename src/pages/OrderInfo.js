import React from 'react';
import { MenuWrapper } from '../common/Styled';

const OrderInfo = () => {
  return (
    <MenuWrapper>
      OrderInfo
    </MenuWrapper>
  );
};

export default OrderInfo;